export const SET_ALBUM = (state, albumInfo) => {
    state.albumInfo = albumInfo;
}
export const SET_SEARCH = (state, albumInfo) => {
    state.albumInfo = albumInfo;
}
export const SEARCH = (state, search) => {
    state.search = search
}